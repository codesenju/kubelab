apiVersion: v1
kind: PersistentVolume
metadata:
  name: harness-data
  labels:
    service: harness-data
spec:
  capacity:
    storage: 5Gi
  nfs:
    server: 192.168.0.16
    path: /mnt/pool1/AppData
  accessModes:
    - ReadWriteOnce
  mountOptions:
    - noatime
    - nfsvers=4.2
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: harness-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      service: harness-data
  storageClassName: ""