apiVersion: v1
kind: Service
metadata:
  name: cloudbeaver
spec:
  selector:
    app: cloudbeaver
  ports:
    - protocol: TCP
      port: 8978
      targetPort: 8978
      name: http-cloudbeaver-1
  type: ClusterIP
