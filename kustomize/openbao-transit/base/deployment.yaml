apiVersion: apps/v1
kind: Deployment
metadata:
  name: openbao-transit
  labels:
    app: openbao-transit
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: openbao-transit
  template:
    metadata:
      labels:
        app: openbao-transit
    spec:
      initContainers:
        - name: copy-config
          image: busybox:1.36
          command:
            - sh
            - -c
            - cp /openbao/config-source/openbao.hcl /openbao/config/openbao.hcl
          volumeMounts:
            - name: config-source
              mountPath: /openbao/config-source
              readOnly: true
            - name: config
              mountPath: /openbao/config
      containers:
        - name: openbao-transit
          image: IMAGE_PLACEHOLDER
          args:
            - server
            - -config=/openbao/config/openbao.hcl
          securityContext:
            capabilities:
              add:
                - IPC_LOCK
          ports:
            - name: http
              containerPort: 8200
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          volumeMounts:
            - name: config
              mountPath: /openbao/config
            - name: data
              mountPath: /openbao/data
      volumes:
        - name: config-source
          configMap:
            name: openbao-transit-config
        - name: config
          emptyDir: {}
        - name: data
          persistentVolumeClaim:
            claimName: openbao-transit-data
