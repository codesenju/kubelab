apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nexus-repo
spec:
  parentRefs:
    - name: local-gateway
      namespace: gateway-infra
      sectionName: websecure
  hostnames:
    - "nexus.local"
  rules:
    - backendRefs:
        - name: nexus-repo
          port: 80