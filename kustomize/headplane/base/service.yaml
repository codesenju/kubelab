apiVersion: v1
kind: Service
metadata:
  name: headplane
  labels:
    app: headplane
spec:
  selector:
    app: headplane
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
